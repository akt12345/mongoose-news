// logic what is actually scraping from news site
// be using cheerio (cant be used alone) used with html - npm package to get html (request, axios(REACT & frontend))
// passes info to fetch controller
